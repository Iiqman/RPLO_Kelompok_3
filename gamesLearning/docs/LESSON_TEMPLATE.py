"""
Lesson Template

Copy this template when adding new lessons to `database.py`.

Fields:
 - `id`: integer unique identifier
 - `title`: short title displayed in UI
 - `description`: (optional) description for the lesson
 - `blocks`: list of strings representing draggable words
 - `correct_order`: list of integers representing the index order that forms the correct sentence

Example:

```python
{
	'past_custom_1': {
		'id': 9,
		'title': 'Past Simple - Custom',
		'description': 'Contoh menambahkan lesson baru',
		'blocks': ['She', 'bought', 'a', 'new', 'book'],
		'correct_order': [0,1,2,3,4]
	}
}
```

When adding the lesson to `database.py`, make sure `id` is unique.
"""

# See the original LESSON_TEMPLATE.py in the project history if needed.
